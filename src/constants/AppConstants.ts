const prefix = '@swipe:';

export default {
  UNVERIFIED_EMAIL: `${prefix}unverifiedEmail`,
  BIOMETRIC_LOCK_ENABLED: `${prefix}:biometricLockEnabled`,
  MARKETING_CONSENT: `${prefix}marketingConsent`,
  APPLICATION_LOCALISED_DATA_TYPE: `${prefix}applicationLocalisedDataType`,
  APPLICATION_REFRESH_ID_TOKEN: `${prefix}applicationRefreshIdToken`,
};

export const styleConstants = {
  bold: '600',
  smallTextSize: 14,
  buttonRadius: 8,
  inputRadius: 6,
  inputHeight: 48,
};
